/**
 * Configuração do Supabase
 * ------------------------
 * Depois de criar seu projeto gratuito em https://supabase.com,
 * troque os dois valores abaixo pelos dados do SEU projeto:
 *
 *   1. No painel do Supabase, vá em "Project Settings" > "API".
 *   2. Copie o "Project URL" e cole em SUPABASE_URL.
 *   3. Copie a chave "anon public" e cole em SUPABASE_ANON_KEY.
 *
 * Enquanto esses valores estiverem com o texto padrão abaixo,
 * o site funciona sozinho em "modo demonstração" (dados de exemplo,
 * sem salvar nada de verdade) — é só pra você ver o visual e testar
 * a navegação antes de conectar o banco de dados de verdade.
 */
window.SUPABASE_URL = "https://hdhqziqvrthxtgyraemk.supabase.co";
window.SUPABASE_ANON_KEY = "sb_publishable_kR7pCuqZrPAr24Xdr0F4Nw_t1j5YUKN";

// Ambiente: branch dev (staging) - usado para iteracao rapida antes de ir pra producao
